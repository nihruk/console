#!/usr/bin/env bash

set -Eeuo pipefail

cd "$(dirname "$0")/.."

docker exec console_app composer install

docker exec info_nihr_db opt/mssql-tools/bin/sqlcmd -S localhost -U SA -P 'Ff4rtB4gsFTWasl0IH8s3qu3ls3' -i  usr/src/sql/setupTestDB.sql