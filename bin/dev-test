#!/usr/bin/env bash

set -Eeuo pipefail

cd "$(dirname "$0")/.."

suite="${1:-build}"

if [ "$suite" == "web" ]; then
  docker exec ioda_php composer run test-phpunit-web
else
  docker exec ioda_php composer run test
  docker exec luceneparser npm run validate-spec
fi
